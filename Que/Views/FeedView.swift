import SwiftUI

struct FeedView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Anasayfa")
                    .font(.largeTitle.bold())
                Spacer()
            }
        }
    }
} 