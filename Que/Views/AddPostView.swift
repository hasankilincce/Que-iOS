import SwiftUI

struct AddPostView: View {
    var body: some View {
        NavigationStack {
            VStack {
                Text("Gönderi Ekle")
                    .font(.largeTitle.bold())
                Spacer()
            }
        }
    }
} 